{% extends 'base.html.twig' %}

{% block body %}
    <h1>Articlecategories list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for articleCategory in articleCategories %}
            <tr>
                <td><a href="{{ path('category_show', { 'id': articleCategory.id }) }}">{{ articleCategory.id }}</a></td>
                <td>{{ articleCategory.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('category_show', { 'id': articleCategory.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('category_edit', { 'id': articleCategory.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('category_new') }}">Create a new articleCategory</a>
        </li>
    </ul>
{% endblock %}
